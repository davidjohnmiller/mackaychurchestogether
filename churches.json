    ---
    layout: nil
    ---

    [
    {% for Church in site.churches %}
        {
          "Name"        : "{{ Church.Name }}",
          "Denomination": "{{ Church.Denomination }}"
        } {% if forloop.last %}{% else %},{% endif %}
    {% endfor %}
    ] 